pipeline {
    agent any

    environment {
        DOCKER_IMAGE = "flaskapp:v1"
        APP_PATH = "${WORKSPACE}/flask-app-cicd-master/web"
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building Docker image'
                sh """
                    docker build -t ${DOCKER_IMAGE} ${APP_PATH}
                    sleep 3
                """
            }
        }

        stage('Test') {
            steps {
                echo 'Running Unit Tests'
                sh 'sleep 3' // Replace with pytest or your test commands
            }
        }

        stage('Integration Tests') {
            steps {
                echo 'Running Integration Tests'
                sh 'sleep 3' // Replace with actual integration tests
            }
        }

        stage('Deployment Preparation') {
            steps {
                echo 'Preparing Deployment'
                sh """
                    docker rm -f flaskapp || true
                    sleep 3
                """
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying Flask App'
                sh "docker run -d --name flaskapp -p 5000:5000 ${DOCKER_IMAGE}"
            }
        }
    }

    post {
        always {
            echo 'Pipeline finished'
        }
        success {
            echo 'Deployment successful!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}